"""Data source clients for the weather plugin.

All sources are free + no-auth + responsibly rate-limited (hourly
poll per location is well within everyone's published terms). Stdlib-
only HTTP so the plugin has zero pip dependencies.
"""
